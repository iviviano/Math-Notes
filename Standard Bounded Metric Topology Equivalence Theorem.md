---
tag: topology
mathLink: the standard bounded metric induces the same topology as is corresponding metric
---
```ad-thm
Let $X$ be a [[Metric Space]] with [[Metric]] $d$. Then, the [[Standard Bounded Metric]] $\bar{d}$ is a [[metric that induces the same [[Topology]] as $d$. 
```
